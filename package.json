{
    "name": "shopexpress-modern",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "deploy": "npm run build && wrangler pages deploy dist"
    },
    "devDependencies": {
        "vite": "^5.0.0",
        "prettier": "^3.0.0"
    },
    "dependencies": {
        "@fortawesome/fontawesome-free": "^6.5.1",
        "flickity": "^2.3.0"
    }
}